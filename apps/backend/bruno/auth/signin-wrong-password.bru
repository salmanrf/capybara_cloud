meta {
  name: signin - wrong password (400)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/auth/signin
  body: json
  auth: none
}

body:json {
  {
    "email": "{{email}}",
    "password": "definitely-wrong"
  }
}
