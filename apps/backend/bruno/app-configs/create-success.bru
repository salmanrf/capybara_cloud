meta {
  name: create app config - success
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/applications/{{app_id}}/configs
  body: json
  auth: none
}

body:json {
  {
    "port": 3000,
    "variables": {}
  }
}
