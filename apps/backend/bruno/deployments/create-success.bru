meta {
  name: create deployment - success (multipart bundle)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/applications/{{app_id}}/deployments
  body: multipartForm
  auth: none
}

body:multipart-form {
  bundle: @file(/Users/m/projects/capybara_cloud/apps/backend/internal/deployment/samples/node-express.tar.gz) @contentType(application/gzip)
}
